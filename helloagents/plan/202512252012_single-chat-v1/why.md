# 为什么做（单聊业务 v1）

- 目标：补齐“单聊”闭环（WS 发送/投递 + REST 历史/查询 + 幂等 + 会话建连策略），用于实习面试展示。
- 现状：
  - WS 收发与幂等（Caffeine）已具备，消息入库/投递基本可用；
  - 缺少 REST 历史查询与对外 API；无单元测试；
  - 分支保护当前要求 Code Owner 审核，单人开发阻塞合并。
- 约束：不改表结构（沿用 t_single_chat / t_message），仅支持 TEXT。
